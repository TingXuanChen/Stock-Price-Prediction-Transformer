# Stock-Price-Prediction-Transformer
This project implements a stock price prediction model for TSMC (2330.TW) based on the Transformer (Encoder-Decoder) architecture. Moving beyond traditional RNN/LSTM approaches, this model leverages the Self-Attention mechanism to capture long-range dependencies within financial time-series data.
